# i18n Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 為遊戲加入英文/中文雙語支援，在 Landing 畫面左上角提供語系切換按鈕。

**Architecture:** 建立單一 `src/i18n/i18n.js` 模組，包含所有翻譯和 `t(key, params?)` 函式。語系存 localStorage。各檔案 import `t` 取代硬編碼字串。切換語系時重啟當前場景。

**Tech Stack:** Phaser 3, JavaScript ES Modules, Playwright

---

### Task 1: 建立 i18n 模組

**Files:**
- Create: `src/i18n/i18n.js`

**Step 1: 建立 i18n.js**

```javascript
const translations = {
    en: {
        // Boot
        loading: 'Loading...',
        loadingPct: 'Loading... {pct}%',
        // Landing
        startGame: 'Start Game',
        quit: 'Quit',
        // HUD
        waveCount: 'Wave {wave}',
        nextWave: 'Next wave: {sec}s',
        waveIncoming: 'Wave incoming!',
        victory: 'VICTORY',
        victoryHud: 'VICTORY!',
        defeat: 'DEFEAT',
        defeatHud: 'DEFEATED',
        // End game
        playAgain: 'Play Again',
        mainMenu: 'Main Menu',
        // Build menu
        goldMine: 'Gold Mine',
        house: 'House',
        barracks: 'Barracks',
        archery: 'Archery',
        tower: 'Tower',
        monastery: 'Monastery',
        // Training
        train: 'Train ({cost}g)',
        // Income/heal popup
        goldIncome: '+{amount}g',
        healAmount: '+{amount}',
    },
    zh: {
        // Boot
        loading: '載入中...',
        loadingPct: '載入中... {pct}%',
        // Landing
        startGame: '開始遊戲',
        quit: '離開',
        // HUD
        waveCount: '第 {wave} 波',
        nextWave: '下一波：{sec}秒',
        waveIncoming: '敵波來襲！',
        victory: '勝利',
        victoryHud: '勝利！',
        defeat: '戰敗',
        defeatHud: '戰敗',
        // End game
        playAgain: '再玩一次',
        mainMenu: '主畫面',
        // Build menu
        goldMine: '金礦',
        house: '房屋',
        barracks: '兵營',
        archery: '射箭場',
        tower: '箭塔',
        monastery: '修道院',
        // Training
        train: '訓練 ({cost}g)',
        // Income/heal popup
        goldIncome: '+{amount}g',
        healAmount: '+{amount}',
    }
};

let currentLocale = localStorage.getItem('locale') || 'en';

export function t(key, params = {}) {
    const dict = translations[currentLocale] || translations.en;
    let str = dict[key] || translations.en[key] || key;
    for (const [k, v] of Object.entries(params)) {
        str = str.replace(`{${k}}`, v);
    }
    return str;
}

export function getLocale() {
    return currentLocale;
}

export function setLocale(locale) {
    currentLocale = locale;
    localStorage.setItem('locale', locale);
}
```

**Step 2: Commit**

```bash
git add src/i18n/i18n.js
git commit -m "feat: create i18n module with en/zh translations"
```

---

### Task 2: LandingScene — 使用 t() + 加入語系切換按鈕

**Files:**
- Modify: `src/scenes/LandingScene.js`

**Step 1: 替換硬編碼字串並加入語系按鈕**

完整替換 LandingScene.js 內容：

```javascript
import { VIEWPORT_WIDTH, VIEWPORT_HEIGHT } from '../config/gameConfig.js';
import { t, getLocale, setLocale } from '../i18n/i18n.js';

export default class LandingScene extends Phaser.Scene {
    constructor() {
        super('LandingScene');
    }

    create() {
        const cx = VIEWPORT_WIDTH / 2;
        const cy = VIEWPORT_HEIGHT / 2;

        // Full-screen background
        const bg = this.add.image(cx, cy, 'landing_bg');
        bg.setDisplaySize(VIEWPORT_WIDTH, VIEWPORT_HEIGHT);

        // --- Language toggle button (top-left) ---
        const langBtn = this.add.image(40, 40, 'ui_btn_sq_blue')
            .setDisplaySize(50, 50).setInteractive();
        const langLabel = getLocale() === 'en' ? 'EN' : '中';
        const langText = this.add.text(40, 40, langLabel, {
            fontSize: '18px', color: '#fef3c0', fontFamily: 'Arial',
            stroke: '#3a2a14', strokeThickness: 3
        }).setOrigin(0.5);

        langBtn.on('pointerover', () => langBtn.setTexture('ui_btn_sq_hover'));
        langBtn.on('pointerout', () => langBtn.setTexture('ui_btn_sq_blue'));
        langBtn.on('pointerdown', () => {
            langBtn.setTexture('ui_btn_sq_pressed');
            const newLocale = getLocale() === 'en' ? 'zh' : 'en';
            setLocale(newLocale);
            this.scene.restart();
        });

        // --- Start Game button ---
        const startBtnY = cy + 120;
        const startBtn = this.add.image(cx, startBtnY, 'ui_btn_blue')
            .setScale(1.2, 0.9).setInteractive();
        this.add.text(cx, startBtnY - 2, t('startGame'), {
            fontSize: '22px', color: '#fef3c0', fontFamily: 'Arial',
            stroke: '#3a2a14', strokeThickness: 3
        }).setOrigin(0.5);

        startBtn.on('pointerover', () => startBtn.setTexture('ui_btn_hover'));
        startBtn.on('pointerout', () => startBtn.setTexture('ui_btn_blue'));
        startBtn.on('pointerdown', () => {
            startBtn.setTexture('ui_btn_blue_pressed');
            this.scene.start('GameScene');
        });

        // --- Quit button ---
        const quitBtnY = startBtnY + 60;
        const quitBtn = this.add.image(cx, quitBtnY, 'ui_btn_blue')
            .setScale(1.2, 0.9).setInteractive();
        this.add.text(cx, quitBtnY - 2, t('quit'), {
            fontSize: '22px', color: '#fef3c0', fontFamily: 'Arial',
            stroke: '#3a2a14', strokeThickness: 3
        }).setOrigin(0.5);

        quitBtn.on('pointerover', () => quitBtn.setTexture('ui_btn_hover'));
        quitBtn.on('pointerout', () => quitBtn.setTexture('ui_btn_blue'));
        quitBtn.on('pointerdown', () => {
            quitBtn.setTexture('ui_btn_blue_pressed');
            window.close();
        });
    }
}
```

**Step 2: Commit**

```bash
git add src/scenes/LandingScene.js
git commit -m "feat: i18n LandingScene with language toggle button"
```

---

### Task 3: BootScene — 使用 t()

**Files:**
- Modify: `src/scenes/BootScene.js`

**Step 1: 替換載入文字**

加入 import：
```javascript
import { t } from '../i18n/i18n.js';
```

將第 20 行的 `'Loading...'` 改為 `t('loading')`。

將第 26 行的 `` `Loading... ${Math.round(value * 100)}%` `` 改為 `t('loadingPct', { pct: Math.round(value * 100) })`。

**Step 2: Commit**

```bash
git add src/scenes/BootScene.js
git commit -m "feat: i18n BootScene loading text"
```

---

### Task 4: GameScene endGame — 使用 t()

**Files:**
- Modify: `src/scenes/GameScene.js`

**Step 1: 替換勝敗彈窗文字**

加入 import（在檔案頂部）：
```javascript
import { t } from '../i18n/i18n.js';
```

第 176 行：將 `result === 'victory' ? 'VICTORY' : 'DEFEAT'` 改為 `result === 'victory' ? t('victory') : t('defeat')`

第 183 行：將 `'Play Again'` 改為 `t('playAgain')`

第 199 行：將 `'Main Menu'` 改為 `t('mainMenu')`

**Step 2: Commit**

```bash
git add src/scenes/GameScene.js
git commit -m "feat: i18n GameScene end game popup"
```

---

### Task 5: HUD — 使用 t()

**Files:**
- Modify: `src/ui/HUD.js`

**Step 1: 替換 HUD 文字**

加入 import：
```javascript
import { t } from '../i18n/i18n.js';
```

第 30 行：`'Wave 0'` → `t('waveCount', { wave: 0 })`

第 36 行：`'Next wave: 60s'` → `t('nextWave', { sec: 60 })`

第 51 行：`` `Wave ${wave}` `` → `t('waveCount', { wave })`

第 57 行：`'Wave incoming!'` → `t('waveIncoming')`

第 60 行：`` `Next wave: ${Math.ceil(seconds)}s` `` → `t('nextWave', { sec: Math.ceil(seconds) })`

第 66 行：`'VICTORY!'` → `t('victoryHud')`

第 70 行：`'DEFEATED'` → `t('defeatHud')`

**Step 2: Commit**

```bash
git add src/ui/HUD.js
git commit -m "feat: i18n HUD text"
```

---

### Task 6: BuildMenu — 使用 t()

**Files:**
- Modify: `src/ui/BuildMenu.js`

**Step 1: 替換建築名稱**

加入 import：
```javascript
import { t } from '../i18n/i18n.js';
```

將 items 陣列中的 name 欄位替換（第 65-70 行）：
- `'Gold Mine'` → `t('goldMine')`
- `'House'` → `t('house')`
- `'Barracks'` → `t('barracks')`
- `'Archery'` → `t('archery')`
- `'Tower'` → `t('tower')`
- `'Monastery'` → `t('monastery')`

**Step 2: Commit**

```bash
git add src/ui/BuildMenu.js
git commit -m "feat: i18n BuildMenu building names"
```

---

### Task 7: 訓練按鈕和浮動文字 — 使用 t()

**Files:**
- Modify: `src/entities/Barracks.js:38`
- Modify: `src/entities/Archery.js:38`
- Modify: `src/entities/Monastery.js:38`
- Modify: `src/ui/UnitPanel.js:46`
- Modify: `src/entities/GoldMine.js:43`
- Modify: `src/entities/Monk.js:100`

**Step 1: 替換各檔案的硬編碼文字**

每個檔案都需要加入 import：
```javascript
import { t } from '../i18n/i18n.js';
```

**Barracks.js** 第 38 行：
`` `Train (${WARRIOR_COST}g)` `` → `t('train', { cost: WARRIOR_COST })`

**Archery.js** 第 38 行：
`` `Train (${ARCHER_COST}g)` `` → `t('train', { cost: ARCHER_COST })`

**Monastery.js** 第 38 行：
`` `Train (${MONK_COST}g)` `` → `t('train', { cost: MONK_COST })`

**UnitPanel.js** 第 46 行：
`` `Train (${WARRIOR_COST}g)` `` → `t('train', { cost: WARRIOR_COST })`

**GoldMine.js** 第 43 行：
`` `+${GOLDMINE_INCOME}g` `` → `t('goldIncome', { amount: GOLDMINE_INCOME })`

**Monk.js** 第 100 行：
`` '+' + MONK_HEAL_AMOUNT `` → `t('healAmount', { amount: MONK_HEAL_AMOUNT })`

**Step 2: Commit**

```bash
git add src/entities/Barracks.js src/entities/Archery.js src/entities/Monastery.js src/ui/UnitPanel.js src/entities/GoldMine.js src/entities/Monk.js
git commit -m "feat: i18n training buttons and floating text"
```

---

### Task 8: Playwright 測試

**Files:**
- Create: `tests/specs/11-i18n.spec.js`

**Step 1: 建立 i18n 測試**

```javascript
import { test, expect } from '@playwright/test';
import { waitForLanding, waitForGameReady, collectPageErrors } from '../helpers/gameHelper.js';

test.describe('i18n', () => {
    test('default locale is English', async ({ page }) => {
        const errors = collectPageErrors(page);
        await waitForLanding(page);

        // Landing scene should be active
        const isActive = await page.evaluate(() =>
            window.game.scene.isActive('LandingScene')
        );
        expect(isActive).toBe(true);
        expect(errors).toHaveLength(0);
    });

    test('switching locale persists and restarts scene', async ({ page }) => {
        await waitForLanding(page);

        // Switch to zh
        await page.evaluate(() => {
            localStorage.setItem('locale', 'zh');
            window.game.scene.getScene('LandingScene').scene.restart();
        });
        await page.waitForTimeout(500);

        const locale = await page.evaluate(() => localStorage.getItem('locale'));
        expect(locale).toBe('zh');

        // Landing scene should still be active after restart
        const isActive = await page.evaluate(() =>
            window.game.scene.isActive('LandingScene')
        );
        expect(isActive).toBe(true);
    });

    test('game works in zh locale', async ({ page }) => {
        await waitForLanding(page);

        // Set zh locale before entering game
        await page.evaluate(() => {
            localStorage.setItem('locale', 'zh');
        });

        // Go to game
        await page.evaluate(() => {
            window.game.scene.start('GameScene');
        });
        await page.waitForFunction(() => {
            return window.gameAPI &&
                   window.game.scene.isActive('GameScene');
        }, { timeout: 10000 });
        await page.waitForTimeout(500);

        const hasAPI = await page.evaluate(() => typeof window.gameAPI !== 'undefined');
        expect(hasAPI).toBe(true);
    });
});
```

**Step 2: 執行全部 Playwright 測試**

Run: `npx playwright test`
Expected: 所有測試通過

**Step 3: Commit**

```bash
git add tests/specs/11-i18n.spec.js
git commit -m "test: add i18n tests"
```
